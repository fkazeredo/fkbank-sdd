# Local development against compose.dev.yaml (PostgreSQL on 127.0.0.1:5432).
fkbank:
  security:
    seeded-login:
      username: e2e.user
      password: e2e-password

logging:
  level:
    org.springframework.security: INFO
