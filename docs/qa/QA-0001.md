# QA-0001 — Ledger core (SPEC-0001)

**Expected duration: ~45 min.** Independent QA, risk R3 (money + DOMAIN invariants M1–M5).
Branch `feature/spec-0001-ledger-core`, head `f7bf0f9`. Environment: real PostgreSQL 16 via
Testcontainers and a throwaway PostgreSQL 16 container for direct-SQL attacks. Docker 29.5.3,
Temurin/Microsoft JDK 21.0.11.

QA-authored artifacts:

- `backend/src/test/java/com/fkbank/acceptance/ledger/LedgerAcceptanceIT.java` (20 tests)
- `backend/src/test/java/com/fkbank/acceptance/ledger/LedgerRaceAcceptanceIT.java` (race suite)
- `docs/tests/TB-0001.md` (test book)

---

## Pass 1 — Black box (FROZEN before any production file was opened)

Everything below was produced without reading a single line of `backend/src/main/**`. The
ledger's published API was recovered from the compiled classes with `javap` (the contract, not
the implementation) and the schema was recovered by applying `V1`/`V2` to a throwaway container
and reading `information_schema` — the migration text itself was never opened.

### Environment and API surface

```
$ docker run -d --name qa-ledger-probe ... postgres:16
$ docker cp backend/src/main/resources/db/migration/V1__baseline.sql qa-ledger-probe:/tmp/v1.sql
$ docker cp backend/src/main/resources/db/migration/V2__ledger.sql   qa-ledger-probe:/tmp/v2.sql
$ docker exec qa-ledger-probe psql -U app -d app -v ON_ERROR_STOP=1 -f /tmp/v1.sql   # ok
$ docker exec qa-ledger-probe psql -U app -d app -v ON_ERROR_STOP=1 -f /tmp/v2.sql   # ok
```

Chart of accounts as seeded, matching `docs/DOMAIN.md` §Chart of accounts for the five static
accounts (the per-customer, per-box and per-loan accounts are opened dynamically, as specified):

```
 id |             code             |        kind
----+------------------------------+---------------------
  1 | internal:settlement:boleto   | INTERNAL_SETTLEMENT
  2 | internal:settlement:pix      | INTERNAL_SETTLEMENT
  3 | internal:settlement:card     | INTERNAL_SETTLEMENT
  4 | internal:expense:yield       | INTERNAL_EXPENSE
  5 | internal:credit:disbursement | INTERNAL_CREDIT
```

Published ledger API (`javap`): `openAccount`, `record`, `reverse`, `balanceOf`, `findPosting`,
`trialBalance`. No REST surface, no DTO, no screen — consistent with the spec's Out of scope.

### Acceptance criteria, executed literally

Command (real PostgreSQL via Testcontainers, never an in-memory profile):

```
$ ./mvnw -o verify -Dit.test='LedgerAcceptanceIT,LedgerRaceAcceptanceIT' \
    -Dtest='LedgerAcceptanceIT' -DfailIfNoTests=false \
    -Dsurefire.failIfNoSpecifiedTests=false -Djacoco.skip=true
```

| # | Acceptance criterion | Result | Evidence |
|---|---|---|---|
| 1 | Posting moves both balances; each = Σ its postings; Σ debits = Σ credits | **PASS** | `LedgerAcceptanceIT.postingMovesBothBalancesAndKeepsTheBooksBalanced` — stored balance compared against an independently written `sum(credit) - sum(debit)` query over `posting` |
| 2 | Customer debit below zero ⇒ `INSUFFICIENT_FUNDS`, nothing written | **PASS** | `refusesToOverdrawACustomerAccountAndWritesNothing` (posting count, balance-row count and both amounts unchanged), `refusesToOverdrawABoxAccount`, `allowsAnInternalAccountToGoNegative`, `allowsDrainingToExactlyZero` |
| 3 | Reversal writes a contra referencing #N; balances restored; #N immutable | **PASS** | `reversalWritesAContraAndRestoresBothBalances`, `theOriginalRowIsNeverMutatedByItsReversal` (full row compared before/after via `select *`) |
| 4 | Race under real PostgreSQL: exactly one commits, other `INSUFFICIENT_FUNDS`, balance ≥ 0, Σ debits = Σ credits | **PASS** | `LedgerRaceAcceptanceIT.exactlyOneOfTwoConcurrentDrainsCommits` (×3), `opposingTransfersDoNotDeadlockOrLoseAnUpdate` (×3) |
| 5 | Verification flags a tampered balance, flags none on a clean ledger | **PASS** | `verificationFlagsATamperedBalance`, `verificationDetectsAPostingSmuggledInByRawSql`, `verificationIsNotRecomputedFromTheBalancesItAudits` |
| 6 | `Money`: half-up at the edge, 4 internal decimals, N-way split sums back | **PASS** | `moneyRoundsHalfUpOnlyAtTheEdge`, `moneyDoesNotRoundMidCalculation`, `splitNeverLosesOrInventsAFraction` (65 combinations), `fourDecimalsSurvivePersistence` |
| 7 | Repeat/nested reversal ⇒ `REVERSAL_NOT_ALLOWED`, nothing written | **PASS sequentially / FAILS under concurrency** | `refusesASecondReversalAndWritesNothing` and `refusesToReverseAReversal` pass; see BB-1 |

`LedgerAcceptanceIT`: **20 tests, 0 failures**. `LedgerRaceAcceptanceIT`: every money-invariant
assertion passes; the two error-contract tests fail deterministically (BB-1).

### Adversarial layer — what I tried and whether it worked

| Attack | Outcome |
|---|---|
| Drive a customer account negative through `record` | **Blocked** — `INSUFFICIENT_FUNDS`, nothing written |
| Drive a box account negative | **Blocked** |
| Drive a customer negative by reversing a deposit already spent | **Blocked** — `INSUFFICIENT_FUNDS`; the refused reversal does not burn the single-reversal allowance (`aRefusedReversalCanStillBePerformedOnceFundsReturn`) |
| Drive a customer negative by racing a reversal against a spend | **Blocked** — exactly one commits, balance ends at zero |
| Double spend by racing two drains | **Blocked** — exactly one commits (×3) |
| Reverse the same posting twice, sequentially | **Blocked** — `REVERSAL_NOT_ALLOWED` |
| Reverse the same posting twice, racing | **Money blocked, error contract broken** — exactly one contra always; wrong exception type (BB-1) |
| Reverse a reversal | **Blocked** in Java; **allowed by the schema** (BB-3) |
| `UPDATE` a posting via raw JDBC | **Blocked** by the append-only trigger |
| `DELETE` a posting via raw JDBC | **Blocked** by the append-only trigger |
| `TRUNCATE posting` via raw SQL | **SUCCEEDED — whole ledger wiped** (BB-2) |
| Insert a second contra for one original via raw SQL | **Blocked** by `posting_reverses_at_most_once_idx` |
| Insert an unbalanced posting via raw SQL | Allowed (by design — `posting` is append-only), and **detected** by `trialBalance` |
| `UPDATE balance SET amount = -50000` on a customer account | **SUCCEEDED** (BB-4) — detected by `trialBalance` |
| `ALTER TABLE posting DISABLE TRIGGER` as the application user | **SUCCEEDED** (BB-4) |
| Post zero, negative, or an account against itself | **Blocked** |
| Reverse an unknown posting id | **Blocked** |

### Black-box findings (frozen)

**BB-1 — MEDIUM — A concurrent repeat reversal never returns `REVERSAL_NOT_ALLOWED`.**

BR-6 and acceptance criterion 7 promise the stable code `REVERSAL_NOT_ALLOWED` for a repeat
reversal, and `docs/DOMAIN.md` §Verifiable invariants states the same. Sequentially that holds.
Under concurrency the loser of the race gets a *different* error, in two variants, both
reproduced 5/5 — deterministic, not flaky:

- *Reversed amount no longer available* → `InsufficientFundsException`:
  `account 69 holds BRL 0.0000 and cannot cover BRL 30.0000`
- *Funds available* → the raw persistence error escapes the ledger API:
  `org.springframework.dao.DataIntegrityViolationException ... constraint
  [posting_reverses_at_most_once_idx] <- org.hibernate.exception.ConstraintViolationException
  <- org.postgresql.util.PSQLException`

The money is safe in both cases — exactly one contra exists, balances are correct and
`trialBalance` stays consistent. What breaks is the published error contract every future money
rail will consume: a duplicate refund would surface to the customer as "insufficient funds", or
as an unmapped infrastructure exception (an HTTP 500 rather than a clean business error). It
also means a Spring `dao` type crosses out of a `domain` API call.

Repro (both are QA-owned, already committed and currently failing by design):

```
$ ./mvnw -o verify -Dit.test='LedgerRaceAcceptanceIT#theLoserOfAReversalRaceGetsTheStableDomainError' ...
$ ./mvnw -o verify -Dit.test='LedgerRaceAcceptanceIT#theLoserOfAFundedReversalRaceGetsTheStableDomainError' ...
Tests run: 5, Failures: 5
```

**BB-2 — MEDIUM — `TRUNCATE posting` bypasses the append-only guarantee and empties the ledger.**

M1 ("append-only ledger ... enforced by a DB trigger") and acceptance criterion 3 ("row #N stays
immutable, no UPDATE/DELETE") are enforced by a row-level `BEFORE DELETE OR UPDATE` trigger.
PostgreSQL row-level triggers do not fire on `TRUNCATE`, and the application's own database role
owns the table:

```
=== ATTACK 1: UPDATE a posting ===
ERROR:  posting is append-only: UPDATE on posting is not permitted
=== ATTACK 2: DELETE a posting ===
ERROR:  posting is append-only: DELETE on posting is not permitted
=== ATTACK 3: TRUNCATE posting ===
TRUNCATE TABLE
=== postings remaining ===
 postings
----------
        0
```

The trigger function is already `TRUNCATE`-aware — it produced the correct message the moment a
statement-level trigger was attached — so only the registration is missing. Verified remedy
(QA-executed on the probe container, offered to the builder as a direction, not as an edit):

```sql
CREATE TRIGGER posting_no_truncate BEFORE TRUNCATE ON posting
  FOR EACH STATEMENT EXECUTE FUNCTION posting_is_append_only();
```

```
=== PROPOSED FIX: statement-level TRUNCATE guard ===
CREATE TRIGGER
ERROR:  posting is append-only: TRUNCATE on posting is not permitted
=== postings after the guarded TRUNCATE attempt ===  postings = 2
```

**BB-3 — LOW (observation) — The schema permits a reversal chain; only Java forbids it.**

`posting_does_not_reverse_itself` blocks self-reference and
`posting_reverses_at_most_once_idx` blocks two contras for one original, but nothing stops a row
whose `reverses_posting_id` points at a posting that is itself a reversal:

```
=== ATTACK 6: reverse a REVERSAL at DB level (chain) ===
INSERT 0 1
 22222222-... |
 33333333-... | 22222222-...
 55555555-... | 33333333-...
```

BR-6 only requires the index to back the *at-most-once* half, so this is within spec. It carries
no race window either — `isReversal` is a property of an immutable row, so the Java check cannot
go stale. Recorded because a later rail could reasonably assume the database enforces both halves.

**BB-4 — LOW (observation) — Nothing in the schema stops a negative customer balance, and the
application role can disable the guard.**

```
=== ATTACK 4b: drive a CUSTOMER balance negative by direct UPDATE ===
UPDATE 1
 account_id |        kind        |   amount
        100 | CUSTOMER_AVAILABLE | -50000.0000

=== ATTACK 7: disable the append-only trigger as the application user ===
ALTER TABLE
```

The `balance` table carries no `kind`, so a plain `CHECK` cannot express "customer accounts only";
the specified mechanism (BR-5 pessimistic locks + the domain guard) is what holds the line, and
`trialBalance` does detect the resulting drift. Both facts are ordinary consequences of the
application owning its schema. Recorded as defence-in-depth context for the security gate rather
than as a defect against this spec.

### Black-box conclusions

- Criteria 1–6 pass literally, in the environment each names.
- Criterion 7 passes sequentially and holds its *money* guarantee under concurrency, but not its
  *error contract* (BB-1).
- I could not make a customer or box account go negative by any application route: not by
  posting, not by reversing a spent deposit, not by racing two drains, not by racing a reversal
  against a spend. Direct SQL can, and the verification routine catches it.
- I could not get a posting reversed twice, nor mutate or delete one through the application or
  through raw `UPDATE`/`DELETE`. `TRUNCATE` is the one hole (BB-2).
- `trialBalance` genuinely reads the `posting` table: it flags drift even when the totals are
  deliberately kept equal, so it is not recomputing its way into agreeing with itself.
- `Money` did not round mid-calculation in any probe, kept 4 decimals through the PostgreSQL
  round trip, and no N-way split lost or invented a fraction across 65 combinations.

--- END OF FROZEN BLACK-BOX BLOCK ---

---

## Pass 2 — White box (opened only after the block above was written)

### Root cause of BB-1, confirmed in the code

`backend/src/main/java/com/fkbank/domain/ledger/Ledger.java:73-85` runs both reversal guards
*before* any lock exists:

```java
public Posting reverse(PostingId original) {
  Posting source = postings.findById(original)...;
  if (source.isReversal()) { throw ReversalNotAllowedException.isItselfAReversal(original); }
  if (postings.existsReversalOf(original)) { throw ReversalNotAllowedException.alreadyReversed(original); }
  return write(Posting.reverseOf(PostingId.next(), source, clock.instant()));
}
```

The lock is taken later, inside `write` then `applyToBalances` (`Ledger.java:143-152`). The
losing thread therefore evaluates `existsReversalOf` on a stale read, blocks on the balance row,
and is let through only after the winner has committed — but never re-evaluates the guard. It
then fails at whichever step comes first: the debit (`Balance.java:48`) or the `INSERT` against
`posting_reverses_at_most_once_idx`.

`V2__ledger.sql:48-54` shows the gap was understood but stopped one step short:

> The ledger refuses a second reversal before it gets here, but that check reads the table and
> then writes to it: two concurrent reversals can both read "not yet reversed". This index is
> what makes the second one fail.

The index does make it fail — it just does not make it fail *as* `REVERSAL_NOT_ALLOWED`. BR-6
requires the rule to be "enforced in the ledger (not delegated to callers)"; surfacing an
untranslated `DataIntegrityViolationException` is precisely delegation to callers.

Because both reversals of one posting always contend on the same two balance rows, re-evaluating
`existsReversalOf` *after* the locks are held is sufficient and deterministic; translating the
constraint violation is the belt-and-braces half. Direction only — the builder owns the fix and
the regression test.

### Transactions and locking

- `LedgerConfig.java:46-53` wraps `Ledger` in a `TransactionInterceptor` with
  `MatchAlwaysTransactionAttributeSource`, so every public method runs in a transaction. That is
  what makes `trialBalance()` read a consistent snapshot and what keeps `SELECT ... FOR UPDATE`
  held for the length of a posting.
- `applyToBalances` sorts the two ids and locks in ascending order (`Ledger.java:145-147`),
  matching BR-5. The debit is applied before the credit, so a refusal happens before anything is
  written.
- **Lock mutation re-verified independently.** The path-guard hook correctly refused a QA edit to
  `BalanceJpaRepository.java`, so the mutation was performed on an isolated copy of the backend
  outside the repository. With `@Lock(PESSIMISTIC_WRITE)` removed:

  | Test | Result without the lock |
  |---|---|
  | `LedgerConcurrencyIT.exactlyOneOfTwoConcurrentDrainsCommits` (builder) | fails — `[COMMITTED, COMMITTED]` |
  | `LedgerConcurrencyIT.opposingTransfersDoNotDeadlock` (builder) | fails — expected `BRL 85.0000` |
  | `LedgerRaceAcceptanceIT.exactlyOneOfTwoConcurrentDrainsCommits` (QA) | fails — `expected: 1L but was: 2L` (x3) |
  | `LedgerRaceAcceptanceIT.opposingTransfersDoNotDeadlockOrLoseAnUpdate` (QA) | fails — `BRL 60.0000` and `BRL 125.0000` where `BRL 85.0000` is correct (x3) |

  With the file untouched — the repository was never modified, `git status` shows only QA-owned
  additions — the same selection is green: `LedgerConcurrencyIT` 2/2 and the QA race suite 12/12.
  The builder's attestation is accurate: the race tests derive their power from the lock.

### Tests: do they carry real assertions

`LedgerSchemaIT` is the strongest of the builder's suites — raw JDBC, going around the
application, and it asserts the value survived the refused `UPDATE` rather than only that an
exception was thrown. `LedgerVerificationIT` corrupts by raw SQL and asserts that `isBalanced()`
stays true while the per-account check fires, which is the right way to show the two checks are
not redundant. `FlywayBaselineIT` was correctly rewritten rather than deleted. No assertion-free
test was found, with the one exception noted in QA-05.

### Migration

`V2__ledger.sql` is additive and does not touch the applied `V1`. Its constraints are meaningful:
`posting_amount_positive`, `posting_between_two_accounts`, `posting_does_not_reverse_itself`, the
partial unique index, and the append-only trigger. `event_publication` is created under Flyway
rather than auto-generated, as DL-0004 intends. The gap is QA-02.

### Architecture and conventions

- `ArchitectureTest.onlyTheLedgerTouchesBalances` is a real M5 rule and passes.
- No source file added or changed by this slice cites `CLAUDE.md`, a `SPEC-`, `OR-`, `BR-`, `DL-`
  or `ADR-` identifier, so the comment convention holds. Two `OR-5` citations exist in
  `backend/pom.xml`, but `git log -S` attributes them to SPEC-0018 — pre-existing, out of scope.
- `Money` never rounds mid-calculation: the only rounding is scale normalisation to 4 in the
  compact constructor and the explicit `atEdge()`, which returns a plain `BigDecimal` rather than
  a `Money`, so a 2-decimal value cannot be stored back as an amount.

---

## Findings

| # | Severity | Finding | Where |
|---|---|---|---|
| QA-01 | MEDIUM | A concurrent repeat reversal never returns `REVERSAL_NOT_ALLOWED`; it returns `INSUFFICIENT_FUNDS` or leaks a raw `DataIntegrityViolationException` out of the domain API | `Ledger.java:73-85` with `Ledger.java:143-152` |
| QA-02 | MEDIUM | `TRUNCATE posting` bypasses the append-only trigger and empties the ledger | `V2__ledger.sql:71-73` |
| QA-03 | LOW | `fkbank.ledger.postings` counts postings that were rolled back | `LedgerMetrics.java:35-42` |
| QA-04 | LOW | The suite is order-coupled: `LedgerSchemaIT` writes postings with no matching balances, leaving drift that is unrepairable because postings are append-only, while `LedgerVerificationIT.flagsNothingOnACleanLedger` asserts `driftedAccounts()` is empty globally | `LedgerSchemaIT.java:148-163` vs `LedgerVerificationIT.java:70` |
| QA-05 | LOW (obs) | `isBalanced()` cannot fail: `sumOfDebitLegs()` and `sumOfCreditLegs()` are the identical query over a schema where one row carries both legs | `PostingJpaRepository.java:21-26` |
| QA-06 | LOW (obs) | The schema permits a reversal chain; only Java forbids reversing a reversal | `V2__ledger.sql:55-61` |
| QA-07 | LOW (obs) | No database-level guard on a negative customer balance, and the application role can `ALTER TABLE ... DISABLE TRIGGER` | `V2__ledger.sql:80-84` |
| QA-08 | LOW (obs) | Unknown account, posting or balance raises `NoSuchElementException` — a JDK type with no stable code — from an API whose other failures are typed `LedgerException`s | `Ledger.java:77, 97, 155` |

### QA-01 — reproduction

```
$ ./mvnw -o verify -Dit.test='LedgerRaceAcceptanceIT#theLoserOfAReversalRaceGetsTheStableDomainError' \
    -Dtest=LedgerAcceptanceIT -DfailIfNoTests=false -Dsurefire.failIfNoSpecifiedTests=false -Djacoco.skip=true
Tests run: 5, Failures: 5
  expected: ReversalNotAllowedException
  but was:  InsufficientFundsException: account 69 holds BRL 0.0000 and cannot cover BRL 30.0000

$ ./mvnw -o verify -Dit.test='LedgerRaceAcceptanceIT#theLoserOfAFundedReversalRaceGetsTheStableDomainError' ...
Tests run: 5, Failures: 5
  but was: org.springframework.dao.DataIntegrityViolationException ... constraint
           [posting_reverses_at_most_once_idx] <- ConstraintViolationException <- PSQLException
```

### QA-02 — reproduction and validated remedy

```
$ docker exec qa-ledger-probe psql -U app -d app -c "TRUNCATE posting CASCADE;" \
    -c "SELECT count(*) FROM posting;"
TRUNCATE TABLE
 count = 0
```

```sql
CREATE TRIGGER posting_no_truncate BEFORE TRUNCATE ON posting
  FOR EACH STATEMENT EXECUTE FUNCTION posting_is_append_only();
```

```
ERROR:  posting is append-only: TRUNCATE on posting is not permitted
```

The existing trigger function needs no change — it already reports `TG_OP` correctly.

### QA-03 — reproduction

```
$ ./mvnw -o verify -Dit.test='LedgerMetricAcceptanceIT' ...
LedgerMetricAcceptanceIT.aRolledBackPostingIsNotCounted:78
  [a movement that never committed must not be reported as one that did]
  expected: 1.0 but was: 2.0
```

The rollback correctly left no posting row and no money; only the counter moved. `@EventListener`
fires inside the transaction, and `postings.save(...)` defers the `INSERT` to flush, so the event
is published before the row exists. `@TransactionalEventListener(AFTER_COMMIT)` is the usual
remedy — the builder's call.

### Not re-run

The battery attested in `dev-verification.md` (`verify-slice`, 114 unit + 40 integration tests,
ArchUnit, JaCoCo, frontend, doc language) was not repeated. The mutation floor (DL-0014, PIT
`UNKNOWN_ERROR`) was not pursued: it is a declared open gap awaiting an owner decision and the
attestation already records the crash. No E2E was run — I agree with the builder that this slice
adds no route and no screen, so the nginx edge is untouched.

---

## Verdict (run 1 of 2): FAIL_REWORK

Money integrity held against every attack I could construct. I could not make a customer or box
account go negative by any application route, could not double-spend, could not get a posting
reversed twice, and could not mutate or delete a posting through the application or through raw
`UPDATE`/`DELETE`. The pessimistic lock is genuinely load-bearing, proven by independent
mutation. `trialBalance` really does audit the postings rather than agree with itself. `Money`
holds its four decimals and its half-up edge. That is a solid accounting core.

Rework is nonetheless required for two defects, both with executable repros and both cheap:

- **QA-01** breaks the error contract that acceptance criterion 7 and BR-6 state, and lets a
  Spring `dao` exception escape a `domain` API. Every money rail in the roadmap will consume this
  API; the cost of leaving it grows with each one.
- **QA-02** leaves a demonstrated bypass of M1, the append-only invariant, with a one-statement
  remedy already validated against the real schema.

QA-03 and QA-04 are recommended in the same cycle, both being small. QA-05 through QA-08 are
observations for the record and need no action to close this slice.

Per `.claude/rules/qa-ownership.md` the failing QA tests stay QA-owned: the builder writes the
regression tests from these repros and does not edit the acceptance suite.

---

# QA run 2 of 2 — re-verification of the rework

**Expected duration: ~35 min.** Scope: confirm or refute each finding from run 1 against commit
`31b7f61`, and look for regressions the fixes may have introduced. This is the final QA run
permitted for this slice; no further cycle exists.

This is a re-verification, not a fresh audit. The full battery attested in `dev-verification.md`
(`./mvnw -o clean verify` → BUILD SUCCESS, 91 unit + 89 integration, JaCoCo floors) was not
repeated. What was run is what the build does not run: the acceptance criteria through the
QA-owned suites, a direct-SQL attack battery against the reworked schema, an out-of-repository
mutation of the fixes to test whether the builder's new regression tests have any power, and the
integration battery in a deliberately different class order.

## Rework diff reviewed

`git show 31b7f61` — five production files: `Ledger.java` (lock/apply split, guard moved under the
lock, typed lookups), `UnknownAccountException` and `UnknownPostingException` (new),
`LedgerMetrics.java` (`@TransactionalEventListener(AFTER_COMMIT)`), `V2__ledger.sql`
(statement-level `posting_no_truncate` trigger). Four test files, plus the three QA-owned
acceptance suites, which the builder committed unchanged.

**QA-owned suites were not tampered with.** `LedgerAcceptanceIT`, `LedgerRaceAcceptanceIT` and
`LedgerMetricAcceptanceIT` appear as additions in `31b7f61` only because run 1 left them
uncommitted. Their content matches what run 1 authored: the two reversal-race contract tests still
assert `ReversalNotAllowedException` on the loser, and `aRolledBackPostingIsNotCounted` still
drives the rollback through an outer `TransactionTemplate`. The suite was made green by fixing the
code, not by weakening the assertions — demonstrated below, since those same tests still fail
against the pre-fix code.

## Per-finding re-verification

### QA-01 — FIXED

```
$ ./mvnw -o verify -Dit.test='LedgerAcceptanceIT,LedgerRaceAcceptanceIT,LedgerMetricAcceptanceIT' \
    -Dtest='LedgerAcceptanceIT' -DfailIfNoTests=false \
    -Dsurefire.failIfNoSpecifiedTests=false -Djacoco.skip=true
LedgerAcceptanceIT ......... Tests run: 20, Failures: 0
LedgerRaceAcceptanceIT ..... Tests run: 22, Failures: 0
LedgerMetricAcceptanceIT ... Tests run:  2, Failures: 0
Tests run: 44, Failures: 0, Errors: 0 — BUILD SUCCESS
```

`theLoserOfAReversalRaceGetsTheStableDomainError` and
`theLoserOfAFundedReversalRaceGetsTheStableDomainError` failed 5/5 each in run 1 and now pass 5/5
each. Both variants are closed: the loser gets `ReversalNotAllowedException` whether the reversed
amount is still available or not, so no `DataIntegrityViolationException` escapes the domain API,
and `contrasFor(original)` remains exactly 1.

The mechanism is sound for the configured isolation. No isolation level is set anywhere in
`backend/src/main`, so PostgreSQL's default READ COMMITTED applies: the loser blocks on
`lockForUpdate`, is released once the winner commits, and its subsequent `existsReversalOf` query
takes a fresh snapshot that includes the winner's contra. Under READ COMMITTED that re-read is
guaranteed to see it, which is what makes the refusal deterministic rather than probabilistic.

### QA-02 — FIXED, and more completely than the finding asked

Fresh probe container, `V1` + `V2` applied from the reworked migration. Both triggers are
registered, `posting_no_truncate` with `tgtype 34` — BEFORE, TRUNCATE, statement-level:

```
           tgname            | tgtype
-----------------------------+--------
 posting_no_truncate         |     34
 posting_no_update_or_delete |     27
```

```
=== ATTACK A: TRUNCATE posting ===                ERROR: ... TRUNCATE on posting is not permitted
=== ATTACK B: TRUNCATE TABLE posting CASCADE ===  ERROR: ... TRUNCATE on posting is not permitted
=== ATTACK C: TRUNCATE account CASCADE ===        NOTICE: truncate cascades to table "posting"
                                                  ERROR: ... TRUNCATE on posting is not permitted
=== ATTACK D: TRUNCATE posting, balance ===       ERROR: ... TRUNCATE on posting is not permitted
=== ATTACK F: DELETE / UPDATE ===                 ERROR: ... DELETE / UPDATE not permitted
=== SURVIVORS ===  postings = 2 | accounts = 5
```

Attack C is the one worth calling out: `TRUNCATE account CASCADE` reaches `posting` through the
foreign key, and PostgreSQL fires the cascaded table's own `BEFORE TRUNCATE` trigger, so the
indirect path is closed too and the whole statement rolls back. The ledger survives every variant.

`TRUNCATE balance CASCADE` still succeeds (attack E) — see QA-07.

### QA-03 — behaviour FIXED; its builder-owned regression test is vacuous (new finding QA-09)

`LedgerMetricAcceptanceIT.aRolledBackPostingIsNotCounted` failed in run 1 and passes now, and
`aCommittedPostingIsCountedOnce` confirms the counter still moves on commit. The behaviour is
correct.

A side benefit worth recording: with the listener outside the transaction, a failure in the metrics
listener can no longer roll back a committed posting. Under `@EventListener` a counter problem was
capable of undoing money.

The builder's own regression test does not exercise the bug — see QA-09 below.

### QA-04 — FIXED, verified by running the battery in a different order

`insertPosting` now moves both settlement balances to match every posting it writes, and every
failing insert (`refusesNonPositiveAmounts`, the third `refusesASecondReversal` call,
`refusesSelfPosting`) throws before the balance statements run, so it leaves nothing behind either.

Reasoning was not relied on. The integration battery was re-run with the class order reversed:

```
$ ./mvnw -o verify -DrunOrder=reversealphabetical -Dfailsafe.runOrder=reversealphabetical \
    -Dtest='LedgerAcceptanceIT' -DfailIfNoTests=false \
    -Dsurefire.failIfNoSpecifiedTests=false -Djacoco.skip=true
... Ledger schema ............ Tests run: 8, Failures: 0     <- ran 3rd
... Ledger - trial balance ... Tests run: 3, Failures: 0     <- ran 12th
Tests run: 89, Failures: 0, Errors: 0 — BUILD SUCCESS
```

`Ledger schema` (`LedgerSchemaIT`) executed *before* `Ledger - trial balance`
(`LedgerVerificationIT`), which is exactly the order run 1 identified as the one the suite was
silently depending on not happening. `flagsNothingOnACleanLedger` and its global
`driftedAccounts()` assertion hold in that order. All 89 integration tests pass.

### QA-08 — behaviour FIXED; the new contract is asserted by nothing (new finding QA-10)

`UnknownAccountException` and `UnknownPostingException` extend `LedgerException` and carry the
stable codes `UNKNOWN_ACCOUNT` and `UNKNOWN_POSTING`. `NoSuchElementException` is gone from
`Ledger.java`. See QA-10 for the coverage gap.

### QA-05, QA-06, QA-07 — reaffirmed as observations

Unchanged by the rework and still correctly classified. `isBalanced()` (QA-05) remains structurally
incapable of failing, which is a redundancy rather than a defect because the per-account check is
the one that carries the guarantee, and run 1 proved that check genuinely reads the postings. The
reversal chain (QA-06) is still schema-permitted and still carries no race, because `isReversal` is
a property of an immutable row.

QA-07 gains one datum: `TRUNCATE balance CASCADE` succeeds, leaving the postings intact and every
balance row gone. This is consistent with the design — the postings are the truth and `balance` is
a derived convenience — and `trialBalance` detects it. It is worth the owner knowing that no
rebuild-from-postings routine exists yet, so recovering from that state today is a manual job.
Still an observation against this spec, and still material for the security gate rather than for
this slice.

## Regression checks on the rework itself

| Risk probed | Result |
|---|---|
| Ascending-id lock order preserved on the `record` path | **Yes** — `record` → `write(p)` → `write(p, lockBalancesOf(p))`; identical set, identical order |
| Ascending-id lock order preserved on the `reverse` path | **Yes** — `lockBalancesOf` is the single shared method and sorts on `AccountId`, which `implements Comparable`. `Posting.reverseOf` swaps debit and credit, so the contra touches the *same two accounts*; sorting makes the swap irrelevant to lock order |
| Deadlock profile changed by the extra query under lock | **No new cycle possible** — the lock *set* is unchanged and no lock is acquired between existing ones; only one indexed read on `posting_reverses_at_most_once_idx` is inserted between acquisition and mutation. `opposingTransfersDoNotDeadlockOrLoseAnUpdate` passes 3/3 |
| Reversal still refused when funds genuinely are not there | **Yes** — `applyToBalances` still debits before crediting, so `Balance.debit` still raises `InsufficientFundsException`. Precedence is now correct: a duplicate reports `REVERSAL_NOT_ALLOWED`, a genuine shortfall reports `INSUFFICIENT_FUNDS` |
| A refused reversal still does not burn the single-reversal allowance | **Yes** — the guard throws before any write; `aRefusedReversalCanStillBePerformedOnceFundsReturn` passes |
| QA-owned suites weakened to make them green | **No** — content matches run 1, and the same tests still fail against pre-fix code |

## Do the builder's regression tests have power

The fixes were reverted on a copy of `backend/` outside the repository (`git show f7bf0f9:<path>`
restored into the copy), keeping the post-fix tests, and the affected classes were re-run. The
repository itself was never modified; `git status` showed only the QA-owned docs.

| Test | Against pre-fix code | Verdict |
|---|---|---|
| `LedgerConcurrencyIT.concurrentReversalsProduceOneContraAndAStableRefusal` (builder, QA-01) | **FAILS** — `elements not expected` | genuine |
| `LedgerSchemaIT.refusesToTruncateThePostingTable` (builder, QA-02) | **FAILS** | genuine |
| `LedgerMetricsIT` — all three tests (builder, QA-03) | **PASSES 3/3** | **vacuous — QA-09** |
| `LedgerMetricAcceptanceIT.aRolledBackPostingIsNotCounted` (QA) | **FAILS** — `expected: 1.0 but was: 2.0` | genuine |

```
[ERROR] Tests run: 2, Failures: 1 -- in LedgerMetricAcceptanceIT
[ERROR] Tests run: 3, Failures: 1 -- in Ledger - concurrent postings
[INFO]  Tests run: 3, Failures: 0 -- in Ledger metrics          <- notices nothing
[ERROR] Tests run: 8, Failures: 1 -- in Ledger schema
```

## New findings

| # | Severity | Finding | Where |
|---|---|---|---|
| QA-09 | LOW | The regression test written for QA-03 does not reproduce QA-03 and passes against the pre-fix code | `LedgerMetricsIT.ignoresARolledBackPosting` |
| QA-10 | LOW | Nothing asserts the stable codes introduced for QA-08 | `UnknownAccountException` / `UnknownPostingException` |

### QA-09 — the QA-03 regression test cannot fail

`LedgerMetricsIT.ignoresARolledBackPosting` provokes its rollback with an
`InsufficientFundsException` from `ledger.record`. On that path `applyToBalances` throws *before*
`postings.save` and therefore before `events.publish` is ever reached, so no `PostingRecorded`
event is published under either the old or the new listener. The test asserts that a counter which
was never going to move did not move.

Proven, not argued: with `LedgerMetrics` reverted to `@EventListener`, the class passes 3/3.

The bug it is supposed to pin is the one where a posting is recorded successfully and *then* the
surrounding transaction rolls back — which needs an outer transaction, as
`LedgerMetricAcceptanceIT.aRolledBackPostingIsNotCounted` does. The behaviour is therefore not
unprotected: the QA-owned acceptance test is committed, runs in the battery, and fails against the
pre-fix code. But it is protected from the QA side, not where the commit message claims, and
`.claude/rules/qa-ownership.md` puts bug regression tests on the builder. If `LedgerMetricsIT` is
ever read as the guard for this behaviour, it will give false confidence.

### QA-10 — the stable codes from QA-08 are asserted nowhere

`grep -rn "UnknownAccountException\|UnknownPostingException\|UNKNOWN_ACCOUNT\|UNKNOWN_POSTING"
backend/src/test/` returns nothing. The QA acceptance suite's `refusesToReverseAnUnknownPosting`
asserts only `RuntimeException`, deliberately, because when it was written the production code
still threw `NoSuchElementException` and the loose assertion was what kept the observation from
becoming a false failure.

The whole point of QA-08 was to give these failures a stable code that callers can react to. A
revert to `NoSuchElementException` would be caught by nothing today. Low severity — the fix is
correct and the surface is small — but on an R3 slice whose value proposition is a stable error
contract, an untested contract is worth the owner seeing.

## Not re-run

The battery attested in `dev-verification.md` was not repeated in its normal form; the
reverse-order run above covers the 89 integration tests and is a check the build does not perform.
The PIT mutation floor remains an open gap recorded as DL-0014 and escalated to the owner; per this
run's scope it was not pursued. No E2E was run: this slice still adds no route and no screen, so
the nginx edge is untouched.

---

## Final verdict (run 2 of 2): PASS_WITH_OBSERVATIONS

Every finding from run 1 that required action is fixed, and each fix was verified against the
behaviour rather than against the builder's word.

- **QA-01, QA-02, QA-03, QA-04, QA-08 — fixed.** The two race-contract tests that failed 5/5 now
  pass 5/5. The `TRUNCATE` hole is closed on every path including the cascade from `account`. The
  suite is order-independent under a genuinely reversed run of all 89 integration tests.
- **No regression found in the rework.** The lock ordering survives the lock/apply split on both
  paths, the lock set is unchanged so no new deadlock cycle is reachable, and the never-below-zero
  rule still applies to reversals with the refusal reasons now in the right precedence.
- **Two new LOW observations**, both about test quality rather than behaviour: QA-09 (the QA-03
  regression test passes against the broken code) and QA-10 (the QA-08 codes are asserted nowhere).

Neither new observation leaves a behaviour unprotected in the committed suite — QA-03 is covered by
the QA-owned acceptance test — so neither blocks the slice, and no rework cycle remains in any
case. They are recorded for the owner's decision: the honest description of QA-09 is that the
slice's QA-03 guard lives in the QA acceptance suite, and the builder-owned test that carries its
name does nothing. Money integrity, the append-only invariant and the reversal contract all hold
under every attack constructed across both runs.

---

# Closure after independent PR review (main agent, 2026-07-20)

Written by the main agent, not by the QA worker. QA had spent both permitted runs when the
independent review of PR #9 returned `APPROVE_WITH_COMMENTS` with three MEDIUM and three LOW
findings. Under the owner decision recorded as HD-12, the work had returned to the main agent,
which then owns every file including the ones on this side of the boundary.

## Disposition of every QA finding

| ID | Verdict at run 2 | Final |
|---|---|---|
| QA-01 | fixed | fixed |
| QA-02 | fixed | fixed, and extended — the reviewer's cascade path is covered too |
| QA-03 | behaviour fixed, guard vacuous | guard honestly relabelled; the real guard is the QA acceptance test (QA-09) |
| QA-04 | fixed | fixed |
| QA-05 | observation | accepted as a structural property, recorded as DL-0016 |
| QA-06 | observation | closed by a schema trigger (DL-0017) |
| QA-07 | observation | truncate half closed by a schema trigger; the negative-balance guard deliberately left out (DL-0017) |
| QA-08 | behaviour fixed, untested | closed — codes now asserted (QA-10) |
| QA-09 | open, LOW | closed |
| QA-10 | open, LOW | closed |

Nothing QA found was dismissed. The two findings QA judged "owner decision, no cycle left"
(QA-09, QA-10) were fixed rather than carried, and the three QA had recorded as observations were
either closed in the schema or written into the spec's Decision log so they stop living only here.

## What this closure is not

It is not a QA verdict. No third QA pass ran, because policy allows two and both were used. The
evidence behind these closures is the full battery (`./mvnw -o clean verify`: 110 unit + 90
integration, 0 failures) plus the reviewer's own reproductions — weaker than an independent QA
pass, and recorded as weaker rather than dressed up as `PASS`.

The standing QA verdict for SPEC-0001 remains **PASS_WITH_OBSERVATIONS** from run 2. The
observations it named are now closed; the verdict itself is not restated by the agent that acted
on it.

---

# QA run 3 — owner-authorised exception, narrowly scoped

**Expected duration: ~50 min.** Scope: the post-review commit `12ddebf` and the doc commit
`85d39e9`, neither of which carried an independent verdict. Runs 1 and 2 exhausted the two-run
policy limit; the owner authorised this third run specifically because those commits changed
transaction semantics on the money path after the QA budget was spent. This is not a fresh audit
of the slice — findings QA-01 through QA-10 keep their run-2 disposition.

Environment: real PostgreSQL 16 via Testcontainers, plus a throwaway PostgreSQL 16 container
(`qa3-ledger-probe`) with `V1`/`V2` applied for direct-SQL attacks. Docker 29.5.3.

The battery attested in `dev-verification.md` (`./mvnw -o clean verify` → BUILD SUCCESS, 110 unit
+ 90 integration, CI 9/9 green on `85d39e9`) was **not** repeated. What was run is what the build
does not run: the transaction semantics observed from inside the engine, a direct-SQL attack
battery against the two new triggers, and an out-of-repository mutation of `Ledger` to measure
whether the new unit suite has power.

New QA-owned artifacts:

- `backend/src/test/java/com/fkbank/acceptance/ledger/LedgerTransactionSemanticsIT.java` (9 tests)
- `backend/src/test/java/com/fkbank/acceptance/ledger/LedgerOutboxDurabilityIT.java` (1 test)
- `backend/src/test/java/com/fkbank/acceptance/ledger/LedgerAuditBlindSpotIT.java` (1 test,
  **failing by design** — it is the executable repro for QA-11)

## 1 — Transaction semantics after the move to `NameMatchTransactionAttributeSource`

The question that mattered: a name-matching attribute source that silently failed to match would
leave `record` and `reverse` with no transaction at all, while the application still started and
most tests still passed. Reading the `*` mapping and concluding it works is exactly the kind of
evidence this project does not accept, so it was measured twice per operation — once through the
attribute the framework resolves, once through what PostgreSQL itself reports from inside the
transaction that was actually begun (a `TransactionExecutionListener` runs
`select current_setting('transaction_isolation')` on the transaction-bound entity manager).

```
$ ./mvnw -o verify -Dit.test='LedgerTransactionSemanticsIT' -Dtest='LedgerAcceptanceIT' \
    -DfailIfNoTests=false -Dsurefire.failIfNoSpecifiedTests=false -Djacoco.skip=true
Tests run: 9, Failures: 0, Errors: 0 -- in Ledger transaction semantics
```

| Question | Answer | Evidence |
|---|---|---|
| Do `record` and `reverse` still get a transaction at all? | **Yes** | `everyOperationIsTransactional` resolves a non-null attribute for all six public methods; `aMovementBeginsAReadCommittedTransaction` and `aReversalBeginsAReadCommittedTransaction` observe a transaction genuinely begun, named after the joinpoint |
| Were the movements dragged up with the audit? | **No** | `movementsKeepTheDatabaseDefault` — `ISOLATION_DEFAULT`, `PROPAGATION_REQUIRED`, not read-only; PostgreSQL reports `read committed` from inside both |
| Is `trialBalance` genuinely at repeatable read? | **Yes** | `theAuditRunsAtRepeatableRead` — PostgreSQL itself reports `repeatable read` and the transaction is read-only |
| Does `readOnly=true` break or silence the audit? | **No** | `theReadOnlyAuditStillAnswers` (the audit still returns a complete answer) and `theConnectionIsNotLeftReadOnly` (a movement straight after the audit still writes) |
| Does the raised isolation cause serialization failures under the concurrency already exercised? | **No** | `LedgerRaceAcceptanceIT` 22/22, `LedgerConcurrencyIT` 3/3, `LedgerVerificationIT` 3/3 — 52 integration tests, 0 failures, no serialization error and no deadlock. A read-only repeatable-read transaction in PostgreSQL cannot raise one, and the movements never left read committed |

The `*` mapping works. The money path is transactional, and the run-2 reasoning behind QA-01's
fix — the loser re-reads under read committed and therefore sees the winner's contra — still
holds, because `reverse` was left at the database default exactly as the commit message claims.

One gap in the guarantee, recorded as QA-13: the raised isolation is conditional.

## 2 — Attacks on the two new triggers

Probe container, `V1` + `V2` applied. Both triggers registered as intended
(`posting_no_reversal_chain` tgtype 7 = BEFORE INSERT FOR EACH ROW; `balance_no_truncate`
tgtype 34 = BEFORE TRUNCATE FOR EACH STATEMENT).

### `posting_no_reversal_chain` — I could not defeat it

| Route | Outcome |
|---|---|
| Plain chain: `C` reverses `B`, `B` reverses `A` | **Blocked** — `posting ... is a reversal and cannot be reversed` |
| The whole chain in **one multi-row INSERT** (`D`, `E` reverses `D`, `F` reverses `E`) | **Blocked** — a `BEFORE INSERT` row trigger does see rows inserted earlier by the same command, so the ordering hole I went looking for does not exist |
| `INSERT ... SELECT` manufacturing a chain in a single statement | **Blocked** |
| `COPY ... FROM STDIN` with a chain link | **Blocked** — `COPY` fires row triggers |
| Evade the `BEFORE INSERT` trigger by `UPDATE`ing a normal posting into a reversal-of-a-reversal | **Blocked** by `posting_no_update_or_delete` |

No legitimate reversal is wrongly refused: the trigger only fires on a row whose parent is itself
a reversal, which the domain already refuses, and the acceptance suite's reversal cases stay green
(`LedgerAcceptanceIT` 20/20). QA-06 is genuinely closed.

### `balance_no_truncate` — the guard is real, its justification is not

`TRUNCATE balance`, `TRUNCATE balance CASCADE` and `TRUNCATE account CASCADE` are all refused.
But the guard covers `TRUNCATE` only:

```
=== ATTACK 5: DELETE FROM balance (not TRUNCATE) ===
DELETE 7
 balances_left | postings_left
---------------+---------------
             0 |             2
```

A plain `DELETE` reaches precisely the state the migration comment and DL-0017 declare must be
impossible — every posting intact, every balance gone. See QA-12. Worse, and separately, the audit
does not notice: see QA-11.

## 3 — `LedgerTest`: does it have power

Eight mutations of `Ledger.java`, applied on a copy of `backend/` **outside the repository**
(`git status` shows only QA-owned additions; no production file was touched here). Baseline on the
copy: 19/19 green.

| Mutation | Killed by | Result |
|---|---|---|
| M1 — duplicate-reversal guard moved back **above** the lock (reverts QA-01's fix) | `checksForADuplicateUnderTheLock` | **killed** |
| M2 — `.sorted()` removed from `lockBalancesOf` | `locksInAscendingIdOrder` | **killed** |
| M3 — credit applied before debit | `debitsBeforeCrediting`, `refusesToOverdraw` | **killed** |
| M4 — balances read via `find()`, never locked | `readsOnlyUnderTheLock`, `locksInAscendingIdOrder`, `checksForADuplicateUnderTheLock` | **killed** |
| M5 — balance read *before* its lock, lock still taken | `readsOnlyUnderTheLock` | **killed** |
| M6 — read-before-lock only for an account already locked earlier in the test | `readsOnlyUnderTheLock` | **killed** |
| M7 — the audit recomputes from the balances it audits | `derivesFromPostingsOnly`, `flagsDrift`, `flagsAFabricatedBalance` | **killed** |
| M8 — the movement is never announced | `announcesTheMovement`, `reversalRestoresBalances` | **killed** |

Both tests flagged as possibly vacuous carry real weight. `checksForADuplicateUnderTheLock` is
sound because the test clears the fake's lock state before the reversal, so "a probe while holding
locks" is a genuine observation rather than a leftover. `readsOnlyUnderTheLock` survived three
separate read-before-lock mutations, including the one built specifically to exploit the fake's
monotonic lock set — the fixture's own first movement runs with nothing locked, which closes the
window I expected to find.

One honest caveat, not a finding: the fake only sees reads that go through
`BalanceRepository.find`, so a balance read that bypassed the port entirely (raw SQL inside an
adapter) would be invisible to it. That is what `ArchitectureTest.onlyTheLedgerTouchesBalances`
covers.

## 4 — `LedgerEventPublicationIT`: does it prove the registry works

It is not vacuous and it is not passing for an unrelated reason.

- Awaitility does assert: with `SpringLedgerEventPublisher.publish` mutated to a no-op on the
  out-of-repository copy, the test **fails** with
  `ConditionTimeoutException ... [the module listener must actually be invoked, or nothing is
  persisted]`.
- Attribution is sound: `RecordingConsumer` is the only `@ApplicationModuleListener` anywhere in
  the codebase (`LedgerMetrics` uses `@TransactionalEventListener`, which registers no
  publication), so the `event_publication` rows in that context can only be its.

What it does **not** prove is the durability claim its own javadoc makes. Completion is the state
that means a restart would *not* redeliver; a registry that wrote nothing until the consumer
returned would satisfy the same assertion while losing every fact to a crash mid-handling. That
gap is now closed from the QA side by `LedgerOutboxDurabilityIT`, which holds the consumer on a
latch and asserts an **outstanding** (`completion_date IS NULL`) row exists first:

```
Tests run: 1, Failures: 0 -- in Ledger outbox - durability before handling
```

The outbox is genuinely durable before handling. The claim is true; it was simply under-asserted.

## 5 — `LedgerMetricsIT.ignoresARefusedPosting`

The rename is accurate and the honesty note is correct. The test drives an
`InsufficientFundsException` and asserts the counter did not move; `applyToBalances` throws before
`postings.save` and therefore before `events.publish`, so no event is published under either
listener style — exactly what the note says. Its pointer to the acceptance suite is also right:
`LedgerMetricAcceptanceIT.aRolledBackPostingIsNotCounted` is the test that failed against pre-fix
code in run 2 and carries the after-commit guarantee. QA-09 is honestly closed.

## 6 — Workflow citations in source

`BR-6` is gone from `LedgerAcceptanceIT.java:240`. Nothing the diff adds cites a workflow artifact
identifier — checked across `LedgerConfig`, `V2__ledger.sql`, `PostingJpaRepository`, `LedgerTest`,
`LedgerEventPublicationIT`, `LedgerMetricsIT` and the new `pom.xml` comment. The citations that
remain in the tree are pre-existing SPEC-0018 material outside this slice, as recorded in run 1.

## 7 — The main agent's edits to QA-owned files

Checked `git show 85d39e9 -- docs/qa/QA-0001.md docs/tests/TB-0001.md` and
`git show 12ddebf -- .../LedgerAcceptanceIT.java`.

**No assertion was weakened and no finding was misrepresented.** The `LedgerAcceptanceIT` change is
a one-line comment rewrite; all 20 tests and every assertion are byte-identical. Both appended doc
sections are labelled as main-agent work, state plainly that they carry no independent QA verdict,
and the disposition table matches what runs 1 and 2 actually found. The refusal to restate the
verdict on its own behalf is the right call.

One overstatement, corrected below as QA-15: `TB-0001.md` records QA-07 as **Closed** on the
strength of `balance_no_truncate`. The `TRUNCATE` half is closed; the `DELETE` half is not, and the
mitigation that made QA-07 low in the first place — "`trialBalance` detects it" — turns out to be
false. That claim originated in **my own run-2 report**, not in the main agent's edit, and is
corrected here.

## New findings

| # | Severity | Finding | Where |
|---|---|---|---|
| QA-11 | MEDIUM | The audit is blind to a *missing* balance row: `trialBalance()` iterates the balances, so an account with postings and no balance is never examined. Emptying `balance` yields `isConsistent() == true` | `Ledger.java:118-127` |
| QA-12 | LOW (compounding) | `balance_no_truncate` guards `TRUNCATE` only; a plain `DELETE FROM balance` reaches the exact state DL-0017 declares impossible | `V2__ledger.sql:119-124` |
| QA-13 | LOW | `trialBalance`'s repeatable read and read-only are silently dropped whenever it is called inside an existing transaction; DL-0018 records the guarantee unconditionally | `LedgerConfig.java:73-82` |
| QA-14 | LOW | Refusing `TRUNCATE balance` reports `posting is append-only: TRUNCATE on posting is not permitted` — the wrong table | `V2__ledger.sql:122-124` |
| QA-15 | LOW (doc) | Three artifacts state that the trial balance reports total drift when `balance` is emptied. It reports none | `V2__ledger.sql:120-121`, DL-0017, `QA-0001.md:533-536` |

### QA-11 — the audit reports a clean ledger on an erased account

```java
public TrialBalance trialBalance() {
  Map<AccountId, Money> netByAccount = postings.netAmountByAccount();
  List<AccountId> drifted =
      balances.findAll().stream()               // <- driven by the balances
          .filter(balance -> hasDrifted(balance, netByAccount))
```

The comparison is driven by the balance rows. An account present in `netByAccount` but absent from
`balances` is never looked at, so *deleting* a balance is undetectable where *tampering* with one
is caught. `isBalanced()` cannot compensate: both totals are the same query (QA-05/DL-0016), so
they stay equal. The result is a full clean bill of health on a destroyed table.

Repro — `LedgerAuditBlindSpotIT`, committed and **failing by design**:

```
$ ./mvnw -o verify -Dit.test='LedgerAuditBlindSpotIT' -Dtest='LedgerAcceptanceIT' \
    -DfailIfNoTests=false -Dsurefire.failIfNoSpecifiedTests=false -Djacoco.skip=true
LedgerAuditBlindSpotIT.detectsADeletedBalanceRow:52
  [the account still has 25.00 of postings behind it and no balance at all;
   an audit that misses that is an audit no balance guard can lean on]
Expecting ListN:
  []
to contain:
  [8]
```

Not reachable through the product — nothing in the application deletes a balance row, and
`openAccount` always creates one. What makes it worth fixing is that every guard placed around
`balance` in `12ddebf` is justified by this audit noticing, and it does not. Direction only, the
builder owns the fix and the regression test: drive the comparison from the union of the accounts
that have postings and the accounts that have balances, rather than from the balances alone.

### QA-13 — the raised isolation is conditional, and fails silently

`trialBalance` keeps `PROPAGATION_REQUIRED`, so a call made inside an existing transaction joins
it. Spring's `validateExistingTransaction` defaults to false, so the requested isolation and
read-only flag are discarded without an error. Proven, not argued —
`LedgerTransactionSemanticsIT.theAuditDegradesInsideAnOuterTransaction` calls the audit inside a
`TransactionTemplate` and observes no transaction begun for `trialBalance` and
`current_setting('transaction_isolation') = read committed` for the duration.

Latent today: there is no caller, and the ledger has no REST surface. It stops being latent the
moment a money rail audits inside its own service transaction — it will silently get the read
committed behaviour DL-0018 was written to eliminate, with nothing failing. `REQUIRES_NEW` on the
audit (or `setValidateExistingTransaction(true)`) makes the guarantee unconditional or loud.

## Not re-run

The `dev-verification.md` battery and CI (9/9 green on `85d39e9`) were not repeated. The mutation
floor remains a declared open gap and was out of scope for this run by instruction. No E2E: the
slice still adds no route and no screen.

---

## Verdict (run 3, owner-authorised exception): FAIL_REWORK

The change that prompted this run is sound. `trialBalance` really does run at repeatable read,
read-only, as PostgreSQL itself reports; `record` and `reverse` really do still get a transaction
at the database default; the `*` mapping is not silently failing; read-only breaks nothing; and
the raised isolation introduces no serialization failure or deadlock across 52 integration tests
including the full race suite. The reversal-chain trigger held against every route I could build,
including the multi-row and `COPY` orderings it was most likely to miss. `LedgerTest` is not
decorative — eight mutations, eight kills, including both tests flagged as suspect.
`LedgerEventPublicationIT` is genuinely wired, and the outbox is durable before handling.

Rework is required for **QA-11**. It is not a defect the commit introduced, but the commit leans on
it: three artifacts now justify a new database guard with the claim that the audit would report the
damage, and the audit reports nothing at all. An audit with a blind spot is worse than a known
absence of one, because the guards around it are sized on the assumption that it sees. The repro is
committed and failing; the fix is a few lines in `trialBalance`.

QA-12, QA-14 and QA-15 are cheap and belong in the same cycle — QA-15 in particular, because
leaving three documents asserting a mitigation that does not exist is how the next reader sizes the
next guard wrongly. QA-13 is a judgement call for the owner: correct today, silently wrong for the
first caller who audits inside a transaction.

Findings QA-01 through QA-10 keep their run-2 disposition; nothing in this run disturbed them.
